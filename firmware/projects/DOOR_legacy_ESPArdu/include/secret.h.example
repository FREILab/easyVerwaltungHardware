/**
 * @file secret.h
 * @brief Private local WiFi credentials. Excluded from version control.
 * @note Keep this file local as include/secret.h.
 *       Machine configuration (MACHINE_NAME, MACHINE_ID, OTA_HOSTNAME)
 *       is defined per environment in platformio.ini via [pair_*] sections.
 */

#ifndef SECRET_H
#define SECRET_H

#define WIFI_SSID "YOUR_WIFI_SSID"
#define WIFI_PASS "YOUR_WIFI_PASSWORD"

#endif /* SECRET_H */
